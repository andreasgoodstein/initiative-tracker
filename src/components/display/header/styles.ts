import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    container: {
        alignItems: 'center',
        backgroundColor: 'red',
        flexDirection: 'row',
        height: 100,
        justifyContent: 'center',
        width: '100%',
    },
});
